export const dashboardState = {
    totalData: [],
    totalOrders: [],
    uploadSliderBtn: true,
    imageUpload: false,
    sliderImages: []
}

export const dashboardReducer = (state, action) => {
    switch (action.type) {
        default:
            return state
    }
}